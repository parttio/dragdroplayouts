allprojects {
  group = 'org.vaadin.addons'
  version = project.hasProperty('BUILD_VERSION') ? getProperty('BUILD_VERSION') : '1.3-SNAPSHOT';

  apply plugin: 'eclipse-wtp'
}

task wrapper(type: Wrapper) {
    gradleVersion = '3.1'
}