============================ QUICK START =======================================
	
In the root directory of the project type 

	./gradlew jettyRunWar (Linux & Mac)

        gradlew.bat jettyRunWar (Windows)

The projects dependencies will be fetched, sources compiled and tests run. 

The demo application will be running under http://localhost:8080/<project name>

ENJOY!

